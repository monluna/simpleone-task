export interface ITheme {
  text: {
    fontFamily: string;
    secondFamily: string;
    thirdFamily: string;
    fontWeight: string;
    fontSize: string;
  };
  colors: {
    bg: string;
    font: string;
  };
}

